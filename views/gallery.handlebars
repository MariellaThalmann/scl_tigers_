<h2>Gallery</h2>
<button> <a href="/new_post">Create Post</a></button>

{{#posts}}
  <div class="post">
    <div> asdfasdfasd</div>
    <small>{{timestamp}}</small>
    <h1>{{username}}</h1>
    <h2>{{title}}</h2>
    <p>{{description}}</p>
    <a href="/post/{{id}}">Details</a>
  </div>
{{/posts}}